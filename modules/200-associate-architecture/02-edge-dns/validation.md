# Validation

- Direct S3 object access is denied while the CloudFront URL succeeds.
- First and repeated responses record CloudFront headers and cache behavior.
- The cache key excludes irrelevant tracking values.
- Route 53 policy selection names the exact requirement it serves.
- No domain or public hosted zone was purchased.
- Distribution is disabled and deleted; its specific OAC and bucket are absent.
